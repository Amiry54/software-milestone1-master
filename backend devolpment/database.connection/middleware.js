
const db = require('../DB');



let result = await db.raw(`select exists (
  select * // USER 

  let result = await db.raw(`select exists (
  select *  //EQU

  let result = await db.raw(`select exists (
  select * //ORDER


  let result = await db.raw(`select exists (
  select * //RATE

  let result = await db.raw(`select exists (
  select *  //SUPPLI

  let result = await db.raw(`select exists (
  select * //CAT

  let result = await db.raw(`select exists (
  select * //CART


  let result = await db.raw(`select exists (
  select * // EQ ORDER